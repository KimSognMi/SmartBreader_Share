<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pet">



	<insert parameterType="PetDTO" id="addPet">
		insert into pet(
		p_num,userid,p_kkcnumber,p_name,p_age,p_gender,p_birth,p_type,p_photo,p_feature)
		values( pet_seq.nextval,#{userid},#{p_kkcnumber},#{p_name},#{p_age},
		#{p_gender},#{p_birth},#{p_type},#{p_photo},#{p_feature})
	</insert>


	<select id="list" resultType="PetDTO" parameterType="String">

		select
		p_num,userid
		,p_kkcnumber,p_name,p_age,p_gender,p_birth,p_type,p_photo,p_feature
		from
		pet
		where userid = #{userid}
	</select>


	<select id="mlist" resultType="PetDTO" parameterType="String">
		select
		p_num,p_kkcnumber,p_photo, p_name, p_type
		from pet
		where userid =
		#{userid}
	</select>

	<select resultType="PetDTO" parameterType="string" id="mypage">
		select
		p_num,userid
		,p_kkcnumber,p_name,p_age,p_gender,p_birth,p_type,p_photo,p_feature
		from pet
		where p_num = #{p_num}
	</select>


	<update id="updatePet" parameterType="PetDTO">
		update pet
		set
		p_name=#{p_name},
		p_age=#{p_age},
		p_gender=#{p_gender},
		p_birth=#{p_birth},
		p_type=#{p_type},
		p_photo=#{p_photo},
		p_feature=#{p_feature}
		where p_num = #{p_num}
	</update>

	<delete id="delete" parameterType="string">
		delete from pet
		where p_num = #{p_num}
	</delete>


	<select id="petdetail" parameterType="int" resultType="BoardDTO">
		select p_num, userid, p_kkcnumber, p_name, p_age, p_gender, p_birth, p_type,
		p_photo, p_feature
		from pet
		where p_num=#{p_num}
	</select>


</mapper>













